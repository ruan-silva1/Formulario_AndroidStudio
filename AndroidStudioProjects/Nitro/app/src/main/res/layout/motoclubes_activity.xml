<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    android:background="@drawable/borda"
    android:padding="4dp"
    >

    <ImageView
        android:id="@+id/img_motoClube"
        android:layout_width="125dp"
        android:layout_height="125dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_motoclube"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/nome_motoclube"
        android:layout_width="0dp"
        android:gravity="center"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        app:layout_constraintEnd_toEndOf="@id/img_motoClube"
        android:maxLines="1"
        app:layout_constraintTop_toBottomOf="@id/img_motoClube"
        tools:text="@tools:sample/lorem/random"
        tools:ignore="MissingConstraints"
        app:layout_constraintStart_toStartOf="@id/img_motoClube"

        />





</androidx.constraintlayout.widget.ConstraintLayout>